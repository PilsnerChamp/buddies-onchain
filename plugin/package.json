{
  "name": "buddy-onchain",
  "version": "1.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "sync-deployments": "if ls ../onchain/deployments/*.json >/dev/null 2>&1; then rm -rf deployments && mkdir -p deployments && cp ../onchain/deployments/*.json deployments/; else echo 'sync-deployments: no JSON files at ../onchain/deployments — aborting (deployments/ untouched)' >&2; exit 1; fi",
    "sync-shared": "bun scripts/sync-shared.ts",
    "build": "bun run sync-deployments && bun run sync-shared && bun build src/index.ts --target=node --outfile=dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "viem": "^2.48.4"
  }
}
